<!--
  Сниппет для AndroidManifest.xml. Скрипт apply-android-patch.sh инжектит:
    - permissions внутрь <manifest>
    - <service> внутрь <application>
  до того как `tauri android build` стартует.

  Permissions:
   - INTERNET — очевидно
   - FOREGROUND_SERVICE / FOREGROUND_SERVICE_SPECIAL_USE — Android 14+ требует
     явный foreground type для VPN
   - POST_NOTIFICATIONS — для уведомления foreground-сервиса (Android 13+)
   - BIND_VPN_SERVICE НЕ нужен в permissions — он навешивается на сам service
     через android:permission="android.permission.BIND_VPN_SERVICE"
   - CAMERA — для QR-сканирования ключей (plugin-barcode-scanner); плагин уже
     декларирует её в своём AAR-манифесте, дублируем явно для консистентности
     с остальными permissions в этом файле
-->
PERMISSIONS:
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.CAMERA" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

SERVICE:
        <service
            android:name=".WhispVpnService"
            android:exported="false"
            android:stopWithTask="false"
            android:process=":vpn"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn_tunnel" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:process=":vpn"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
